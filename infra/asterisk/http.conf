[general]
enabled=yes
bindaddr=0.0.0.0
bindport=8088
tlsenable=no
; Enable TLS with production certificate paths before exposing WSS.
; tlsenable=yes
; tlsbindaddr=0.0.0.0:8089
; tlscertfile=/run/secrets/asterisk-fullchain.pem
; tlsprivatekey=/run/secrets/asterisk-key.pem
