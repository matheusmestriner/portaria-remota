[general]
enabled=yes
webenabled=no
tlsenable=yes
tlsbindaddr=0.0.0.0:5039
tlscertfile=/run/secrets/asterisk-fullchain.pem
tlsprivatekey=/run/secrets/asterisk-key.pem
; Configure a real account with read=user and no write privileges in the include.
; Restrict permit/deny to the event bridge IP. No manager listener on public ingress.
#tryinclude manager-clients.conf
