name: Validate platform
on: [push, pull_request]
jobs:
  node:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run typecheck
      - run: npm run build
      - run: npm audit --audit-level=high
  agent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache: false
      - run: go test ./...
        working-directory: services/agent
      - run: go build ./...
        working-directory: services/agent
  whatsapp:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          cache: false
      - run: go get go.mau.fi/whatsmeow@2375e17 github.com/lib/pq@v1.10.9
        working-directory: services/whatsmeow
      - run: go mod tidy
        working-directory: services/whatsmeow
      - run: go test ./...
        working-directory: services/whatsmeow
      - run: go build ./...
        working-directory: services/whatsmeow
